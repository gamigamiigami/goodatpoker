import http from 'node:http';import fs from 'node:fs';import path from 'node:path';const base=path.resolve('dist');http.createServer((req,res)=>{const file=path.resolve(base,'.'+decodeURIComponent(new URL(req.url,'http://localhost').pathname));if(!file.startsWith(base+path.sep)&&file!==base){res.writeHead(403).end();return}const f=file===base?path.join(base,'index.html'):file;fs.readFile(f,(e,b)=>{if(e){res.writeHead(404).end();return}res.setHeader('Content-Type',({'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.svg':'image/svg+xml','.webmanifest':'application/manifest+json'})[path.extname(f)]||'application/octet-stream');res.end(b)})}).listen(4173,'127.0.0.1',()=>console.log('Local: http://localhost:4173'));
